name: 更新EPG节目单

on:
  workflow_dispatch:  # 手动触发
  schedule:
  # 北京时间凌晨2点 = UTC时间前一天18点
  - cron: '15 16,18,20,22,0,2,4,6,8,10,12,14 * * *'

jobs:
  sync:
    runs-on: ubuntu-latest
    
    steps:
    - name: 下载原始IPTV文件
      run: |
        wget -q https://epg.112114.xyz/pp.xml -O 112114_1day_epg.xml

    - name: 推送到Gitee
      run: |
        # 配置Git
        git config --global user.name "Auto Sync"
        git config --global user.email "auto@sync.com"
        
        export TZ='Asia/Shanghai'
        # 获取北京时间
        BEIJING_TIME=$(date '+%Y-%m-%d %H:%M:%S')
        
        # 克隆仓库
        git clone https://oauth2:${{ secrets.GITEE_TOKEN }}@gitee.com/tyrian9905/iptv.git
        
        # 进入仓库并更新文件
        cd ${REPO}
        
        # 复制文件
        cp ../112114_1day_epg.xml ./
        
        # 提交并推送
        git add 112114_1day_epg.xml
        git commit -m "自动同步 ${BEIJING_TIME}"
        git push origin master
        
        echo "🎉 节目单EPG成功！"
