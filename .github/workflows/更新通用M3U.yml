name: 更新通用M3U

on:
  workflow_dispatch:  # 手动触发
  schedule:
  # 北京时间凌晨2点 = UTC时间前一天18点
  - cron: '20 1,9 * * *'

jobs:
  process:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests pyyaml
        
    - name: Process Channels
      run: |
        cat <<'EOF'>final_processor.py
        import re, requests
        from urllib.parse import quote

        class TVProcessor:
            __slots__ = ('config', 'skip_pattern')
            
            def __init__(self):
                self.config = {
                    'source': "https://my9.ltd/tv/pllive.txt",
                    'logo': "https://raw.githubusercontent.com/fanmingming/live/main/tv/",
                    'epg': "https://gitee.com/tyrian9905/iptv/raw/master/1day112114_epg.xml"
                }
                self.skip_pattern = re.compile(r'卫视([1-9]|1[0-2])|卫视F|4K|8K', re.IGNORECASE)

            def process(self):
                with (
                    open('MY9原版.m3u', 'w', encoding='utf-8') as f_orig,
                    open('MY9分节目.m3u', 'w', encoding='utf-8') as f_cat,
                    open('MY9无分组.m3u', 'w', encoding='utf-8') as f_flat
                ):
                    # 写入文件头
                    header = f"#EXTM3U x-tvg-url=\"{self.config['epg']}\"\n"
                    f_orig.write(header)
                    f_cat.write(header)
                    f_flat.write(header)

                    content = requests.get(self.config['source']).text.splitlines()
                    
                    # 跳过前两行说明
                    for line in content[2:]:
                        line = line.strip()
                        if not line or '#genre#' in line or line.startswith('#'):
                            continue
                        
                        if ',' not in line:
                            continue
                        
                        original_name, url = line.split(',', 1)
                        original_name, url = original_name.strip(), url.strip()
                        
                        # 第一步：执行所有指定的替换操作
                        processed_name = self._standardize_name(original_name)
                        
                        # 第二步：按照VB6逻辑处理
                        tv_name, group = self._vb6_style_process(processed_name, original_name)
                        
                        # MY9原版：使用处理后的名称但保留原始分组
                        self._write_entry(f_orig, tv_name, "", original_name, url)
                        
                        # 跳过需要过滤的频道（仅用于分节目和无分组版本）
                        if self.skip_pattern.search(processed_name):
                            continue
                        
                        # MY9分节目：分类版
                        self._write_entry(f_cat, tv_name, group, original_name, url)
                        # MY9无分组：统一分组
                        self._write_entry(f_flat, tv_name, "MY9", original_name, url)

            def _standardize_name(self, name):
                """执行所有指定的7次替换操作"""
                name = name.upper().strip()
                replacements = [
                    (' ', ''),
                    ('-', ''),
                    ('HD', ''),
                    ('SD', ''),
                    ('高清', ''),
                    ('标清', ''),
                    ('超清', '')
                ]
                for old, new in replacements:
                    name = name.replace(old, new)
                return name

            def _vb6_style_process(self, processed_name, original_name):
                """按照VB6逻辑处理标准化后的名称"""
                # CCTV处理
                if "CCTV" in processed_name:
                    group = "央视"
                    
                    # 特殊频道处理
                    if "+" in processed_name or "PLUS" in processed_name:
                        return "CCTV5+", group
                    if "3D" in processed_name:
                        return "CCTV3D", group
                    if "美洲" in processed_name or "AME" in processed_name:
                        return "CCTV4美洲", group
                    if "欧洲" in processed_name or "EUO" in processed_name:
                        return "CCTV4欧洲", group
                    if "10" in processed_name:
                        return "CCTV10", group
                    
                    # 处理数字频道
                    matches = re.search(r"(CCTV\d{1,2})[^\d]", processed_name.replace("0", "") + "HD")
                    if matches:
                        return matches.group(1), group
                    
                    # 其他CCTV频道（如CCTV兵器科技）
                    return original_name.replace("CCTV", ""), "专题"
                
                # CETV/CGTN处理
                if "CETV" in processed_name or "CGTN" in processed_name:
                    return original_name, "央视"
                    
                # 卫视处理
                return original_name, "卫视"

            def _write_entry(self, f, tvg_name, group, display_name, url):
                """写入标准格式条目"""
                safe_name = quote(tvg_name.replace("/", ""))
                f.write(
                    f'#EXTINF:-1 tvg-name="{tvg_name}" '
                    f'tvg-logo="{self.config["logo"]}{safe_name}.png" '
                    f'group-title="{group}",{display_name}\n'
                    f"{url}\n"
                )

        if __name__ == '__main__':
            TVProcessor().process()
        EOF

        python final_processor.py
      
    - name: 推送到Gitee
      run: |
        # 配置Git
        git config --global user.name "Auto Sync"
        git config --global user.email "auto@sync.com"
        USER="tyrian9905"    # ← 用户名
        REPO="iptv"        # ← 仓库名
        
        export TZ='Asia/Shanghai'
        # 获取北京时间
        BEIJING_TIME=$(date '+%Y-%m-%d %H:%M:%S')
        
        # 克隆仓库
        git clone https://oauth2:${{ secrets.GITEE_TOKEN }}@gitee.com/${USER}/${REPO}.git
        
        # 进入仓库并更新文件
        cd ${REPO}
        
        # 复制文件
        cp ../MY9原版.m3u ./
        cp ../MY9分节目.m3u ./
        cp ../MY9无分组.m3u ./
        
        # 提交并推送
        git add MY9原版.m3u MY9分节目.m3u MY9无分组.m3u
        git commit -m "自动同步 ${BEIJING_TIME}"
        git push origin master
        
        echo "🎉 节目m3u更新成功！"
