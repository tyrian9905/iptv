name: 处理广西移动M3U同步至Gitee

on:
  workflow_dispatch:  # 手动触发
  schedule:
  # 北京时间凌晨2点 = UTC时间前一天18点
  - cron: '0 18 * * *'  # UTC时间18点执行（北京时间第二天2点）

jobs:
  sync:
    runs-on: ubuntu-latest
    
    steps:
    - name: 下载原始IPTV文件
      run: |
        curl -s -o gxmobile.m3u https://raw.githubusercontent.com/Healer-sys/Home/main/iptv/gx.m3u
        echo "✅ 已下载原始文件，大小: $(wc -l < gxmobile.m3u) 行"

    - name: 过滤超清极清和4K资源
      run: |
        echo "🔍 过滤超清、极清和4K资源..."
        
        # 使用grep反向过滤更高效
        if grep -q "超清\|极清\|纯享4K" gxmobile.m3u; then
          grep -v "超清\|极清\|纯享4K" gxmobile.m3u > gxmobile_SD.m3u
        else
          cp gxmobile.m3u gxmobile_SD.m3u
        fi
        
        echo "✅ 过滤完成"
        echo "原始文件: $(wc -l < gxmobile.m3u) 行"
        echo "过滤后: $(wc -l < gxmobile_SD.m3u) 行"
        
        # 验证结果
        echo "🔍 验证过滤结果："
        if grep -q "超清\|极清\|纯享4K" gxmobile_SD.m3u; then
          echo "❌ 过滤失败，仍包含超清/极清/4K内容"
          exit 1
        else
          echo "✅ 已成功过滤超清、极清和4K资源"
        fi

    - name: 统一处理M3U文件
      run: |
        echo "🔄 统一处理M3U文件（EPG源和名称清理）..."
        
        # 处理两个文件
        for file in gxmobile.m3u gxmobile_SD.m3u
        do
          if [[ ! -f "$file" ]]; then
            echo "⚠️ 文件 $file 不存在，跳过"
            continue
          fi
          
          echo "正在处理: $file"
          
          # 一次性使用sed进行所有替换操作，更高效
          sed -i \
            -e 's|x-tvg-url="[^"]*"|x-tvg-url="https://baoer.org.cn/e.xml"|g' \
            -e 's/tvg-name="[^"]* 超清[^"]*"/tvg-name="&"/g' \
            -e 's/tvg-name="[^"]* 高清[^"]*"/tvg-name="&"/g' \
            -e 's/tvg-name="珠江卫视"/tvg-name="广东珠江"/g' \
            -e 's/ 超清//g' \
            -e 's/ 高清//g' \
            -e 's/ 标清//g' \
            -e 's/ 极清//g' \
            -e 's/ 4K//g' \
            -e 's/标清4M//g' \
            -e 's/(标清)//g' \
            -e 's/-//g' \
            "$file"
          
          # 处理group-title逻辑
          temp_file="${file}.tmp"
          > "$temp_file"
          
          while IFS= read -r line
          do
            # 处理group-title逻辑
                    if [[ "$line" =~ tvg-name=\"([^\"]*)\" ]]; then
                        # 提取tvg-name的值
                        tvg_name="${BASH_REMATCH[1]}"
                        
                        # 检查tvg-name是否包含"卫视"
                        if [[ "$tvg_name" == *"卫视"* ]]; then
                            # 如果存在group-title，替换为"全国卫视"
                            if [[ "$line" =~ group-title=\"([^\"]*)\" ]]; then
                                # 替换现有的group-title
                                line="${line//group-title=\"${BASH_REMATCH[1]}\"/group-title=\"全国卫视\"}"
                            else
                                # 如果没有group-title，添加一个
                                line="${line}, group-title=\"全国卫视\""
                            fi
                        fi
                    fi
                    
                    # 4. 将"本地及特色"更改为"广西热播"
                    if [[ "$line" =~ group-title=\"本地及特色\" ]]; then
                        line="${line//group-title=\"本地及特色\"/group-title=\"广西热播\"}"
                    fi
            
            echo "$line" >> "$temp_file"
          done < "$file"
          
          mv "$temp_file" "$file"
          echo "✅ $file 处理完成"
        done

    - name: 推送到Gitee
      run: |
        # 设置变量
        USER="tyrian9905"    # ← 用户名
        REPO="iptv"        # ← 仓库名
        TOKEN="a74325e9ecd2a8f505b8cd8d9b7138a0"      # ← Token
        
        # 配置Git
        git config --global user.name "Auto Sync"
        git config --global user.email "auto@sync.com"
        
        export TZ='Asia/Shanghai'
        # 获取北京时间
        BEIJING_TIME=$(date '+%Y-%m-%d %H:%M:%S')
        
        # 克隆仓库
        git clone https://oauth2:${TOKEN}@gitee.com/${USER}/${REPO}.git
        
        # 进入仓库并更新文件
        cd ${REPO}
        
        # 复制处理前的文件
        cp ../gxmobile.m3u ./
        
        # 复制处理后的文件
        cp ../gxmobile_SD.m3u ./
        
        # 提交并推送
        git add gxmobile.m3u gxmobile_SD.m3u
        git commit -m "同步并过滤频道 ${BEIJING_TIME}
        - 更新EPG源
        - 清理tvg-name字符：' 超清'、' 高清'、'标清'、' 极清'、' 4K'、'标清4M'、'(标清)'"
        git push origin master
        
        echo "🎉 同步成功！"
        echo "原始文件: https://gitee.com/${USER}/${REPO}/raw/master/gxmobile.m3u"
        echo "过滤后文件: https://gitee.com/${USER}/${REPO}/raw/master/gxmobile_SD.m3u"
        echo ""
        echo "📊 文件信息："
        echo "gxmobile.m3u 大小: $(du -h gxmobile.m3u | cut -f1)"
        echo "gxmobile_SD.m3u 大小: $(du -h gxmobile_SD.m3u | cut -f1)"
